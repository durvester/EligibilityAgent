# Database (Azure PostgreSQL)
DATABASE_URL="postgresql://user:password@host:5432/eligibility_agent?schema=public"

# Practice Fusion SMART on FHIR
PF_FHIR_BASE_URL="https://fhir.practicefusion.com/r4"
PF_CLIENT_ID=""
PF_CLIENT_SECRET=""
PF_AUTHORIZE_URL="https://auth.practicefusion.com/oauth2/authorize"
PF_TOKEN_URL="https://auth.practicefusion.com/oauth2/token"

# Stedi Healthcare API
STEDI_API_KEY=""
STEDI_API_URL="https://healthcare.us.stedi.com/2024-04-01"
STEDI_PARTNER_ID=""

# NPI Registry (public, no auth needed)
NPI_REGISTRY_URL="https://npiregistry.cms.hhs.gov/api"

# App Configuration
NEXT_PUBLIC_APP_URL="http://localhost:3000"
NEXT_PUBLIC_API_URL="http://localhost:3001"  # Direct API URL for SSE (bypasses Next.js proxy)
API_URL="http://localhost:3001"
NODE_ENV="development"
CORS_ORIGIN=""  # Allowed CORS origin (empty = use NODE_ENV default: allow all in dev, none in prod)

# Encryption (generate with: openssl rand -base64 32)
ENCRYPTION_KEY=""

# Session
SESSION_SECRET=""
